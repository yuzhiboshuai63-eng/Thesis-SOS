---
name: paper-reduce
description: >
  中文学术论文智能降重工具，支持四种核心模式：降重复率（深度）、降AI率、降重+降AIGC、AI率预测。
  通过多层级语义改写、句式变换、同义词替换、结构重组等技术，有效降低论文重复率和AI生成痕迹。
  支持知网、维普、万方、PaperPass、格子达等主流检测平台的针对性优化。
  典型触发场景：用户提到"论文降重"、"降低重复率"、"降AI率"、"论文改写"、"降AIGC"、"AI率预测"、
  "论文查重"、"重复率太高"、"论文润色改写"等。
  仅支持中文论文。
---

# 中文学术论文智能降重

## Overview

本 Skill 提供一套完整的中文论文降重工作流，参考业界领先的降重技术（如"一触即改"等平台），通过 AI 驱动的多维度文本改写策略，帮助用户有效降低论文的重复率和 AI 生成痕迹。

### 四大核心模式

| 模式 | 功能 | 适用场景 |
|------|------|----------|
| **降重复率（深度）** | 全网领先的降重技术，一键大幅降低重复率，保证学术表达 | 查重报告显示重复率过高 |
| **降AI率** | 优化文本内容，降低AI生成痕迹，使文章更具人文特色 | AI检测工具标记为AI生成 |
| **降重+降AIGC** | 同时降低重复率和AI生成痕迹，全面提升文章质量 | 两者都需要优化 |
| **AI率预测** | 选择对应算法，比对应平台更严格地预测AI率 | 提交前预估AI检测风险 |

### 支持的检测平台

知网、维普、万方、PaperPass、格子达、PaperYY、笔杆网、华宸、paperred、writepass、papered、大雅、朱雀

## 前置要求

- 无需额外安装依赖
- 支持输入格式：.docx、.txt、.md 或直接粘贴文本
- 输出格式：.docx（保留原文格式）或 .txt

---

## 核心工作流

### Phase 0: 模式选择与配置

**MANDATORY**：在开始任何处理之前，必须先确认以下配置：

1. **选择降重模式**（四选一）：

| 模式 | 说明 | 改写策略侧重 |
|------|------|-------------|
| `reduce-dup` | 降重复率（深度） | 语义改写 + 句式变换 + 同义词替换 |
| `reduce-ai` | 降AI率 | 人性化表达 + 增加个人风格 + 打破AI模式 |
| `reduce-both` | 降重+降AIGC | 综合以上两种策略 |
| `predict-ai` | AI率预测 | 不改写，仅分析和预测 |

2. **确认检测平台**（影响改写策略的严格程度）：

不同平台的检测算法差异显著，改写策略需针对性调整：

| 平台 | 检测特点 | 改写策略建议 |
|------|----------|-------------|
| 知网 | 语义级检测，13字符连续匹配即标红 | 需深度语义改写，不能仅替换同义词 |
| 维普 | 关键词+句子结构双重检测 | 需同时改变句式和关键词 |
| 万方 | 段落级模糊匹配 | 需重组段落结构和逻辑顺序 |
| PaperPass | 严格连续字符匹配 | 需打断连续相同字符 |
| 格子达 | AI检测较严格 | 需强化人性化改写 |
| PaperYY | 综合检测 | 需多维度改写 |
| 其他 | 各有侧重 | 根据平台特点灵活调整 |

3. **输入方式**：
   - 📝 文本输入：直接粘贴待处理文本
   - 📤 文件上传：上传 .docx / .txt / .md 文件

**交互规范**：使用 AskUserQuestion 工具让用户选择模式和平台。如果用户未指定，默认使用 `reduce-dup` 模式和 `知网` 平台。

### Phase 1: 文本输入与预处理

#### 1.1 文本获取

根据用户选择的输入方式获取文本：

- **文本输入**：直接使用用户粘贴的文本
- **文件上传**：读取文件内容
  - `.docx` 文件：使用 python-docx 提取纯文本（保留段落结构）
  - `.txt` / `.md` 文件：直接读取

#### 1.2 文本预处理

对输入文本进行预处理，为后续改写做准备：

```
预处理步骤：
1. 识别文本结构（标题、正文、引用、参考文献等）
2. 标记不可修改区域（专有名词、公式、数据、引用原文等）
3. 按段落/句子切分文本单元
4. 记录原文长度和段落数
```

**CRITICAL**：以下内容**绝对不可修改**：
- ❌ 专有名词（人名、地名、机构名、品牌名等）
- ❌ 专业术语（医学、法律、工程等领域特定术语）
- ❌ 数据和统计数字
- ❌ 公式和数学表达式
- ❌ 法律条文、政策文件原文引用
- ❌ 参考文献条目
- ❌ 已标注的引用内容（引号内的文字）
- ❌ 图表标题和编号

### Phase 2: 智能降重处理

根据选择的模式执行对应的改写策略。

---

#### 模式 A：降重复率（深度）`reduce-dup`

**目标**：大幅降低与已有文献的重复率，同时保证学术表达的准确性和流畅性。

**改写策略层级**（按优先级从高到低执行）：

##### 策略 1：语义深度改写（核心策略）

这是最有效的降重策略，通过重新理解原文含义并用完全不同的方式表达：

```
执行规则：
1. 理解原文核心含义和论证逻辑
2. 用不同的学术表达方式重新阐述
3. 保持原文的学术严谨性和逻辑完整性
4. 确保改写后的文本在语义上与原文等价
```

**改写示例**：

| 原文 | 改写后 | 策略 |
|------|--------|------|
| 本研究采用深度学习技术对图像进行分类 | 通过构建卷积神经网络模型，实现了对输入图像的精确分类与识别 | 语义改写+具体化 |
| 随着互联网技术的快速发展，电子商务已经成为人们日常生活中不可或缺的一部分 | 网络信息技术的持续演进，使得线上交易模式深度融入了大众的消费行为之中 | 语义改写+词汇替换 |
| 实验结果表明，该方法具有较高的准确率和较好的鲁棒性 | 测试数据证实，所提方案不仅精度表现优异，同时在面对各类干扰时也展现出良好的稳定性 | 语义改写+句式变换 |

##### 策略 2：句式结构变换

通过改变句子的语法结构来打破匹配：

```
变换技巧：
1. 主动句 ↔ 被动句
2. 长句拆分 ↔ 短句合并
3. 正装语序 ↔ 倒装语序
4. 单句 ↔ 复合句
5. 陈述句 ↔ 强调句
6. 改变主语（从"本文"改为"该研究"/"此项工作"等）
```

**示例**：

| 原文 | 改写后 | 变换方式 |
|------|--------|----------|
| 本文提出了一种新的算法来解决这个问题 | 针对上述问题，一项全新的算法方案被设计并实现 | 主动→被动 |
| 该方法不仅提高了效率，还降低了成本 | 效率的显著提升与成本的有效控制，是该方案带来的两大核心优势 | 并列句→主谓句 |
| 通过实验验证了算法的有效性 | 为验证所提算法的实际效能，本研究设计并开展了一系列对照实验 | 简单句→扩展句 |

##### 策略 3：同义词与近义词替换

在保持语义不变的前提下替换词汇：

```
替换规则：
1. 优先替换高频学术词汇（如"研究"→"探讨"/"考察"/"分析"）
2. 使用同领域近义表达（如"提高"→"增强"/"改善"/"优化"）
3. 替换连接词和过渡词（如"因此"→"由此可见"/"基于此"）
4. 替换修饰语（如"显著的"→"明显的"/"突出的"/"可观的"）
```

**常用替换词库**（参考 `references/synonym_dict.md`）：

| 原词 | 替换词 |
|------|--------|
| 研究 | 探讨、考察、分析、探究、审视 |
| 提出 | 设计、构建、开发、创立、引入 |
| 结果表明 | 数据证实、分析显示、研究发现、测试揭示 |
| 有效 | 高效、可行、切实、显著 |
| 提高 | 增强、改善、优化、提升、促进 |
| 方法 | 方案、策略、途径、手段、技术路线 |
| 问题 | 难题、挑战、瓶颈、困境 |
| 影响 | 作用、效应、效果、冲击 |
| 重要 | 关键、核心、突出、不可或缺 |
| 使用 | 采用、运用、借助、利用、依托 |

##### 策略 4：段落逻辑重组

对段落内部和段落之间的逻辑进行重组：

```
重组方式：
1. 调整段落内论证顺序（因果→递进、总分→分总）
2. 合并或拆分段落
3. 改变论述角度（从不同视角阐述同一观点）
4. 增加过渡句和衔接词
5. 补充解释性语句（在不改变原意的前提下）
```

##### 策略 5：知网专项优化

针对知网检测算法（13字符连续匹配）的特殊处理：

```
知网专项规则：
1. 确保任意连续13个字符不与原文相同
2. 在关键位置插入学术性修饰语
3. 使用不同的学术表达习惯
4. 适当调整数字和数据的表述方式
5. 改变引用格式的细微差异
```

---

#### 模式 B：降AI率 `reduce-ai`

**目标**：降低AI生成痕迹，使文本更具人文特色和人类写作特征。

**AI文本的典型特征**（需要消除的）：

| AI特征 | 表现 | 消除策略 |
|--------|------|----------|
| 句式过于工整 | 句子长度均匀，结构单一 | 制造长短句交替，增加句式多样性 |
| 过度使用连接词 | "此外"、"另外"、"总之"频繁出现 | 减少机械过渡，使用更自然的衔接 |
| 缺乏个人观点 | 表述过于客观中立 | 适当加入作者视角的判断和评价 |
| 段落结构模式化 | 每段都是"总-分-总" | 打破固定模式，尝试多种段落结构 |
| 词汇选择过于"完美" | 用词精准但缺乏口语化表达 | 适当使用更自然的学术表达 |
| 缺乏具体细节 | 描述过于概括和抽象 | 增加具体的数据、案例和细节 |
| 论述过于平衡 | 正反面论述过于对称 | 适当突出重点，不必面面俱到 |

**人性化改写策略**：

##### 策略 1：打破AI句式模式

```
执行规则：
1. 制造句长变化：将均匀的句子改为长短交替（8-15字短句与25-40字长句混搭）
2. 增加句式多样性：混合使用陈述句、设问句、倒装句、强调句
3. 减少机械过渡词：用语义衔接替代"首先...其次...最后..."
4. 增加口语化学术表达：如"不难发现"、"值得指出的是"、"从某种意义上说"
```

##### 策略 2：增加个人写作风格

```
执行规则：
1. 适当加入作者视角的判断和评价
2. 使用更具个人特色的学术表达
3. 增加过渡性的个人思考（如"笔者认为"、"基于上述分析，我们倾向于..."）
4. 适当使用修辞手法（类比、排比、设问等）
```

##### 策略 3：增加文本"不完美性"

```
执行规则：
1. 适当增加具体案例和实际数据
2. 加入一些领域内的常识性解释
3. 偶尔使用稍微口语化的学术表达
4. 增加一些个人研究经历的暗示
5. 适当使用领域内约定俗成的简称
```

##### 策略 4：降低AI检测特征值

```
执行规则：
1. Perplexity（困惑度）优化：使文本更不可预测，避免过于"标准"的表达
2. Burstiness（突发性）优化：制造句子复杂度的波动
3. 词汇多样性：避免重复使用同一表达方式
4. 语义深度：增加论证层次，避免过于扁平的论述
```

---

#### 模式 C：降重+降AIGC `reduce-both`

**目标**：同时降低重复率和AI生成痕迹。

**执行规则**：

```
1. 先执行模式A（降重复率）的全部策略
2. 在降重结果的基础上，再执行模式B（降AI率）的策略
3. 特别注意：降重后的文本可能引入新的AI特征，需要二次检查
4. 最终输出需同时满足：低重复率 + 低AI检测率 + 学术表达准确
```

**双重改写流程**：

```
原文 → [降重复率处理] → 中间文本 → [降AI率处理] → 最终文本
         (语义改写+句式变换)              (人性化+风格化)
```

---

#### 模式 D：AI率预测 `predict-ai`

**目标**：预测文本在各平台的AI检测率，不改写文本。

**预测维度**：

| 检测维度 | 说明 | 权重 |
|----------|------|------|
| 困惑度（Perplexity） | 文本的可预测程度，AI文本通常困惑度较低 | 30% |
| 突发性（Burstiness） | 句子复杂度的变化幅度，AI文本通常较均匀 | 25% |
| 词汇分布 | 词汇使用的多样性和自然度 | 20% |
| 句式结构 | 句子结构的多样性和复杂度 | 15% |
| 语义连贯性 | 段落间的逻辑衔接自然度 | 10% |

**预测流程**：

```
1. 分析文本的AI特征指标
2. 对照各平台的检测标准进行评估
3. 给出各平台的预估AI率
4. 标注高风险段落和句子
5. 提供针对性的修改建议
```

**输出格式**：

```
AI率预测报告
==================

平台：知网
预估AI率：35%（高风险 ⚠️）
置信度：中等

平台：维普
预估AI率：22%（中风险）
置信度：中等

平台：格子达
预估AI率：45%（高风险 ⚠️）
置信度：较高

--- 风险段落标注 ---

段落 3（高风险）：
"随着人工智能技术的不断发展，深度学习在各个领域得到了广泛应用。"
→ 问题：句式过于工整，连接词使用模式化
→ 建议：改为"近年来，深度学习技术快速迭代，其应用版图已从计算机视觉扩展至自然语言处理等多个方向。"

段落 7（中风险）：
"本文提出了一种新的方法，该方法具有以下优点：首先，效率高；其次，成本低；最后，易于实现。"
→ 问题：典型的AI列举模式（首先...其次...最后...）
→ 建议：改为"所提方案的核心优势体现在三个方面：在运行效率上，较现有方法提升了约30%；在部署成本方面，仅需标准硬件配置即可运行；此外，模块化的设计思路也使得后续维护和功能扩展更为便捷。"
```

---

### Phase 3: 质量检查与校验

**MANDATORY**：改写完成后，必须执行以下质量检查：

#### 3.1 语义完整性检查

```
检查项：
✅ 改写后的文本是否保持了原文的核心含义
✅ 论证逻辑是否完整连贯
✅ 学术结论是否与原文一致
✅ 数据和事实是否准确无误
✅ 引用关系是否正确保持
```

#### 3.2 学术规范检查

```
检查项：
✅ 学术用语是否规范准确
✅ 语法是否正确无误
✅ 标点符号使用是否规范
✅ 专业术语使用是否一致
✅ 人称和时态是否统一
✅ 参考文献引用是否完整
```

#### 3.3 降重效果预估

```
检查项：
✅ 与原文的相似度是否显著降低（目标：相似度降低50%以上）
✅ 是否存在与原文连续相同超过13个字符的情况（知网标准）
✅ 关键句是否已全部改写
✅ 段落结构是否有明显变化
```

#### 3.4 AI特征检查（仅限降AI率/降重+降AIGC模式）

```
检查项：
✅ 句子长度是否有自然变化
✅ 是否存在过度使用连接词的情况
✅ 段落结构是否多样化
✅ 是否有个人写作风格的体现
✅ 词汇使用是否自然多样
```

### Phase 4: 结果输出

#### 4.1 输出格式

根据用户需求选择输出格式：

| 格式 | 说明 | 适用场景 |
|------|------|----------|
| `.docx` | 保留原文格式，改写内容替换原文 | 需要直接提交的论文 |
| `.txt` | 纯文本格式 | 仅需文本内容 |
| 对比报告 | 原文与改写文本的逐句对比 | 需要审核改写质量 |

#### 4.2 输出内容结构

```
输出文件包含：
1. 改写后的完整文本
2. 降重效果摘要（预估重复率变化）
3. 改写策略说明（使用了哪些改写策略）
4. 修改标注（可选：标注修改过的段落）
5. 质量检查报告（语义完整性、学术规范性等）
```

#### 4.3 保存位置

- 最终输出文件保存到 `/workspace/`
- 中间处理文件保存到 `/data/user/work/`

---

## 改写执行规范

### 基本原则

1. **学术第一**：所有改写必须保证学术表达的准确性和严谨性
2. **语义等价**：改写后的文本必须与原文在语义上等价
3. **最小修改**：在达到降重目标的前提下，尽量减少对原文的改动
4. **保持风格**：保持原文的学术风格和写作水平
5. **不可修改区域**：严格遵守不可修改区域的保护规则

### 逐段改写流程

对每个段落执行以下流程：

```
Step 1: 判断段落类型
  - 标题段落 → 仅微调措辞，不改变核心表述
  - 正文段落 → 执行完整改写策略
  - 引用段落 → 不修改
  - 参考文献段落 → 不修改
  - 图表标题 → 不修改

Step 2: 分析段落内容
  - 识别核心论点和支撑论据
  - 标记专有名词和不可修改内容
  - 分析段落内部的逻辑结构

Step 3: 执行改写
  - 按照所选模式的策略执行改写
  - 优先使用高层级策略（语义改写 > 句式变换 > 同义词替换）
  - 对每个句子逐一改写，确保无连续相同字符

Step 4: 质量校验
  - 检查改写后的句子是否通顺
  - 检查学术表达是否准确
  - 检查是否与原文有连续相同字符
```

### 改写强度控制

根据重复率目标调整改写强度：

| 目标重复率 | 改写强度 | 策略组合 |
|-----------|----------|----------|
| < 10% | 深度改写 | 语义改写 + 句式变换 + 段落重组 + 知网专项 |
| 10%-20% | 中度改写 | 语义改写 + 句式变换 + 同义词替换 |
| 20%-30% | 轻度改写 | 句式变换 + 同义词替换 |
| > 30% | 微调 | 仅同义词替换和语序微调 |

---

## 平台针对性策略

### 知网（CNKI）

```
核心挑战：13字符连续匹配算法
改写要点：
1. 每隔10-12个字符必须有至少1个不同的字符
2. 不能仅替换个别词语，需要改变表达方式
3. 数字和单位也需要变换表述（如"85%"→"百分之八十五"或"约八成五"）
4. 专有名词之间的连接词需要变化
```

### 维普

```
核心挑战：关键词+句子结构双重检测
改写要点：
1. 需要同时改变关键词和句子结构
2. 段落首句需要特别注意（维普对段首权重高）
3. 论文标题和摘要的改写需要格外谨慎
```

### 万方

```
核心挑战：段落级模糊匹配
改写要点：
1. 需要重组段落结构和逻辑顺序
2. 段落长度需要有变化
3. 论述角度需要调整
```

### PaperPass

```
核心挑战：严格连续字符匹配
改写要点：
1. 打断所有连续相同字符
2. 使用完全不同的表达方式
3. 适当增加或减少修饰语
```

### 格子达 / PaperYY（AI检测严格）

```
核心挑战：AI生成检测
改写要点：
1. 重点执行降AI率策略
2. 增加人性化表达和个人风格
3. 打破AI文本的典型模式
4. 增加具体细节和案例
```

---

## 常见问题与注意事项

### Q1: 改写后学术性是否会降低？

不会。本 Skill 的改写策略以学术准确性为第一原则。所有改写都经过语义完整性检查，确保改写后的文本在学术表达上与原文等价甚至更优。

### Q2: 降重效果能保证吗？

降重效果取决于多种因素：
- 原文的重复率水平
- 选择的检测平台
- 论文的学科领域
- 改写强度设置

一般来说，使用深度改写策略可以将重复率降低 50%-80%。但对于与自身已发表论文的重复（自引），效果可能有限。

### Q3: 降AI率的效果如何？

降AI率的效果因平台而异。经过人性化改写后，大多数平台的AI检测率可以降低到 10%-20% 以下。但需要注意的是，AI检测技术不断升级，无法保证100%通过。

### Q4: 哪些内容不能修改？

以下内容绝对不可修改：
- 专有名词（人名、地名、机构名）
- 专业术语
- 数据和统计数字
- 公式和数学表达式
- 法律条文、政策文件原文引用
- 参考文献条目
- 已标注的引用内容

### Q5: 一次处理多少字合适？

建议每次处理 3000-5000 字为宜。过长的文本可能导致改写质量下降。对于长篇论文，建议分章节处理。

---

## Resources

### references/
- `synonym_dict.md` — 学术论文常用同义词替换词库，按学科分类整理
- `ai_patterns.md` — AI生成文本的典型模式特征库，用于识别和消除AI痕迹
- `platform_strategies.md` — 各检测平台的算法特点和针对性改写策略详解

### scripts/
- `text_preprocessor.py` — 文本预处理脚本，识别文本结构和不可修改区域
- `quality_checker.py` — 改写质量检查脚本，验证语义完整性和学术规范性
